/// Opt-in shorthand DSL for constructing linear quantities as `value.prefix.unit` or `value.unit`.
///
/// `SIAccessories` re-exports `UtiliseesNonSI` so the shorthand surface can cover both SI and
/// accepted non-SI units from a single import.
@_exported import UtiliseesNonSI
